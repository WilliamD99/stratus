---
import { Image } from "astro:assets";
import aboutImage from "@/assets/clean.png";
---

<section id="about" class="w-full bg-gray-50 py-12 sm:py-20 lg:py-24 xl:py-28">
  <div class="max-w-7xl 2xl:max-w-[100rem] mx-auto px-4 sm:px-6 lg:px-8">
    <div
      class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 xl:gap-16 2xl:gap-36"
    >
      <!-- Left Column: Content -->
      <div class="order-1 lg:order-1">
        <!-- Section Badge -->
        <div class="relative w-fit">
          <p class="text-2xl leading-tight mb-4 font-bold">
            Who We Are
            <span class="h-[2px] w-full bg-stratus-blue block mt-2"></span>
          </p>
        </div>

        <!-- Main Title -->
        <h2
          class="text-3xl sm:text-4xl lg:text-5xl leading-tight mb-6 2xl:mb-10 font-bold"
        >
          Professional Cleaning
          <span class="block text-stratus-blue font-bold">You Can Trust</span>
        </h2>

        <!-- Description -->
        <div class="stratus-body text-lg leading-relaxed space-y-4 mb-8">
          <p>
            At Stratus Janitorial, we specialize in providing high-quality
            cleaning services for medium and high-rise residential buildings. We
            understand that each building is more than just a structure—it’s a
            community. We take pride in ensuring every space we touch is clean,
            welcoming, and cared for with attention to detail.
          </p>
        </div>
      </div>

      <!-- Right Column: Image -->
      <div class="order-2 lg:order-2">
        <div class="relative">
          <!-- Main Image -->
          <div class="relative overflow-hidden rounded-2xl shadow-2xl">
            <Image
              src={aboutImage}
              alt="Professional cleaning team at work"
              class="w-full h-[400px] sm:h-[500px] lg:h-[550px] object-cover brightness-90"
              loading="lazy"
            />
            <!-- Overlay gradient for better text readability if needed -->
            <div
              class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent"
            >
            </div>
          </div>

          <!-- Decorative elements -->
          <div
            class="absolute -top-4 -right-4 w-24 h-24 bg-stratus-blue rounded-full opacity-10"
          >
          </div>
          <div
            class="absolute -bottom-4 -left-4 w-32 h-32 bg-emerald-green rounded-full opacity-10"
          >
          </div>

          <!-- Stats Card -->
          <div
            class="absolute bottom-6 left-6 bg-white rounded-lg shadow-lg p-4 max-w-[200px]"
          >
            <div class="text-center">
              <div class="text-2xl font-bold text-stratus-blue">10+</div>
              <div class="text-sm text-slate-gray">Years of Experience</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
